#include<stdio.h>

int main(){
	const int LENGTH=10;
	const int WIDTH=20;
	const char NEWLINE='\n';

	int area=LENGTH*WIDTH;
	printf("value of area : %d",area);
	printf("newline %c",NEWLINE);

	return 0;
}
